-- TODO Case 1: one row per seller - orders, revenue, avg delivery days, on-time %, avg review
-- score.
-- Hints: join stg_order_items -> fct_orders (delivered) -> stg_sellers; bring reviews per order.
select ?? ?
